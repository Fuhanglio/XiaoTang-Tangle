# 更新说明 · 小唐 Tangle

本仓库基于 [WakeUp课程表](https://github.com/YZune/WakeupSchedule_Kotlin)（作者 YZune / 杨增，Apache License 2.0）二次开发。
下文按版本列出标志性改动，起点是「小卡片更新不了课表」这一问题链，逐版记录做了什么、为什么这么做。

上游项目地址：<https://github.com/YZune/WakeupSchedule_Kotlin>

---

## 起点：问题从哪来

v104 之前，今日课程卡走的是「ListView + 位图行」方案。

v104 把卡片改成静态行，但每一行的内容仍然是先用代码绘制成 Bitmap，再塞进 RemoteViews 下发。4 行位图在同一次 `updateAppWidget` 里约有 1.9MB，超过系统约 1MB 的传输上限，整次更新被系统直接丢弃。

表现就是：卡片停在布局文件里的默认文案，看起来像「加载不出来」「更新不了」。

这条问题链贯穿 v104 到 v107，也是本轮改造的起点。

---

## 总览

| 版本 | versionName | 类型 | 一句话 |
| --- | --- | --- | --- |
| 104 | 3.635 | 改版 | 今日卡首轮静态行改版，行内容仍用位图（引入超限问题） |
| 105 | 3.636 | 重做 | 今日卡彻底去位图，改原生控件行块 + 高度自适应 + 空态 |
| 106 | 3.637 | 重做 | 周课表卡改成「一周概览列表」，去掉贴图方案 |
| 107 | 3.638 | 修复 | 修掉今日卡长期「加载中…」的真根因（刷新入口依赖数据库登记） |
| 108 | 3.639 | 外观 | 三个小部件预览图全部重画，尺寸调整 |
| 109 | 3.640 | 功能 | 周课表卡加「+」直达加课；同名课程颜色自动对齐 |
| 110 | 3.641 | 功能 | 「课程预告范围」二选一；空态短句库 55 条 |
| 111 | 3.642 | 修正 | 预告范围语义修正为「从今天起共 N 天」；短句库扩到 255 条 |
| 112 | 3.643 | 功能 | 新增「生日提醒板块」 |
| 113 | 3.644 | 功能 | 课表网格内**拖拽移动课程** |
| 114 | 3.645 | 功能 | 课表同步到系统日历 |
| 115 | 3.646 | 改版 | 今日卡两种排列：竖排（右侧显示上课时间）/ 紧凑两列 |
| 116 | 3.647 | 修复 | 三处装机反馈修正；补回丢失的生日板块渲染；默认课时 45 分钟 |
| 117 | 3.648 | 修复 | 长课名不再挤掉教室名（课名限行加省略号，教室行优先保住） |
| 118 | 3.649 | 构建 | APK 固定签名，从此每版都能覆盖安装、数据不丢 |
| 119 | 3.650 | 修复 | 修掉「设置生日时莫名跳到选择时间表」的自动跳转缺陷 |
| 120 | 3.651 | 修复 | 茅台学院导入不全：先自动展开「更多」的完整课表再抓取 |
| 121 | 3.652 | 功能 | 加「每日课表」：每天一条全天汇总，和周课表互补 |
| 122 | 3.653 | 功能 | 加「图片导入」：识别课表截图，本机离线 OCR，不联网 |
| 123 | 3.654 | 修复 | 修掉「拨生日滚轮时整页退出到课表设置」：把滚轮区从系统边缘返回手势的判定区里排除 |
| 129 | 3.660 | 修复 | v124~129 端到端代码审查收口：稳定性 + 安全五批修复一次性落地 |
| 130 | 3.661 | 修复 | 修掉「负一屏课程卡没有课」：日历同步适配新版 ColorOS 私有日历库（com.coloros.calendar） |
| 131 | 3.662 | 修复 | 同步日历时自动创建「小唐Tangle课表」本地日历（不再要求手机里先有可写日历）；修课名把教室名顶出格子；图片识别结果列表改两行显示 |
| 141 | 3.672 | 修复 | v140 的「优先写进系统课程表日历」**没生效**：那个日历的 `calendar_access_level` 实测为 **NULL**，被「可写」条件 `CALENDAR_ACCESS_LEVEL >= 500` 静默过滤（`NULL >= 500` 为假）。现按 account_name 单独补进候选并排到最前。日历「课程表」视图与**负一屏日程卡同源**，故一并修复 |
| 140 | 3.671 | 修复 | 三处遗留问题：① **系统日历「课程表」视图空白** —— ColorOS 日历的「课程表」只读它自带的本地日历（`account_name=oplus_course_default_calendar`、显示名「课程表」），我们写进了自建的「小唐Tangle课表」所以视图看不到；改为**只要该日历存在就优先写进它**，并在选择框/完成弹窗里标明 ② 导入方式文案 `从图片导入（识别课表截图）` → `图片导入` ③ 小部件刷新异常（`goAsync` 里被吞掉）新增落盘到「下载」目录，便于定位「载入窗口小部件时出现问题」 |
| 139 | 3.670 | 修复 | **修「导入完成后直接闪退」**（真机 OnePlus 13 / Android 16 抓到 7 次堆栈：`java.util.NoSuchElementException` @ `ScheduleFragment.initWeekPanel`）：`idxList.filter{...}.minBy{...}` 在过滤结果为空时抛异常（全项目唯一的 minBy，紧随其后却写着 `if (pick != null)`，本意即 minByOrNull）——导入的课若整组都早于当前周就会命中。改为 `minByOrNull` |
| 138 | 3.669 | 修复 | **修「导入被误判为非教务站点」**（茅台学院等）：v132 加的 JS 桥站点校验取错了字段（`schoolInfo[1]` 实为校名，且 `schoolInfo` 全工程从未赋值），白名单只剩「已保存的 URL」一条来源，页面一跳转/换子域就误拒。现改为「首次加载 host + 已保存 host + 学校 URL（索引修正为 2）」并放宽为同一主域名即视为同站点（`jwxt.mtxy.edu.cn` ↔ `cse.mtxy.edu.cn` 互通，不会放到 `evil.com`）；万一仍对不上改为**弹框显示实际域名并允许用户确认导入**，不再硬拒绝 |
| 137 | 3.668 | 上架准备 | **应用显示名统一为「小唐Tangle课程表」**（原 app_name=WakeUp课程表；应用内侧边栏大标题本就是「小唐Tangle」，v129 开发日志也记过改名意图，这次在代码里落实），google 渠道变体改「小唐Tangle课程表Pro」，并同步隐私政策草稿名称。注：包名 / 数据库名 wakeup / .wakeup_schedule 后缀 / 日历 UID 前缀 WakeUpSchedule- 均未动 |
| 136 | 3.667 | 修复 | **修「一打开就崩溃」**：AndroidX 版本错配对齐（appcompat→1.6.1、material→1.9.0、core-ktx→1.9.0、constraintlayout→2.1.4、+recyclerview 1.3.2）；tranOldData 整体兜底（原 DB 访问在 try 之外，异常沿 SplashActivity 协程冒泡闪退）；SplashActivity 失败也进主页；新增 CrashLogger 写公共「下载」目录 |
| 135 | 3.666 | 上架准备（二） | **targetSdk 29 → 34**（Android 14）工具链与代码迁移：AGP 3.5.3→8.1.4、Gradle 5.4.1→8.7、JDK 8→17、Kotlin 1.3.61→1.9.22、compileSdk/targetSdk→34；清单 package→namespace；kotlin-android-extensions→ViewBinding 方案（同名扩展属性，零改调用点）；26 文件 synthetics 迁移 + 5 bean Parcelize；PendingIntent 补 FLAG_IMMUTABLE（17 处）；Android 13+ 通知运行时权限；Room 2.2.3→2.6.1、WorkManager 2.3.4→2.9.0、navigation→2.7.7；非传递 R 类关闭；内嵌 splitties 注解改 @RequiresOptIn；包可见性 <queries> |
| 134 | 3.665 | 准备 | 上架准备（一）：清单所有组件显式声明 android:exported（行为不变，为后续 targetSdk 31+ 合规打基础）；忽略本地构建日志 |
| 133 | 3.664 | 修复 | 今日课程卡修复：刷新异常不再杀死进程导致小部件卡在「加载中…」，渲染失败自动推空态卡自愈 |
| 132 | 3.663 | 修复 | 第二轮全量代码审查修复：小部件刷新接入后台执行（补真 ANR 修复）、切课表后渲染旧表、迁移索引名、伪造提醒通道移除、闹钟残留、刷新口径统一、同名课真覆盖、删表连带清理等 20+ 项 |

---

## v104 / 3.635 · 今日卡首轮静态行改版

- 今日课程卡由 ListView 改为静态行布局。
- 行内容改用 Bitmap 绘制后下发。
- 结果：单次更新数据量约 1.9MB，超系统上限被丢弃，卡片表现为空白 / 停在默认文案。

## v105 / 3.636 · 今日卡彻底重做

这是第一版真正可用的今日卡。

- **去掉位图**，行内容改用 RemoteViews 原生的控件行块（TextView + ImageView 组合），单次更新体积降到可以忽略。
- 卡片高度按桌面实际占用自适应，显示几行由 `AppWidgetManager.getAppWidgetOptions()` 的实际高度算出，避免留白和滚动条。
- 逐行着色走 `ImageView.setColorFilter`，ImageView 用白色圆角 drawable 作 src。直接 `setBackgroundColor` 会丢圆角，这条路走不通。
- 卡片背景做成玻璃感（半透明白 + 细描边）。RemoteViews 不支持 `setAlpha`，也做不了真正的背景模糊，只能用这个近似方案。
- 补上无课时的空态显示。
- 新增 `AppWidgetUtils.refreshWidgetById`，修掉「改完课后卡片不刷新」的回归问题。
- 删除 `TodayRowRenderer.kt` 与废弃布局 `today_course_app_widget_1.xml`。

## v106 / 3.637 · 周课表卡改成「一周概览列表」

- `schedule_app_widget.xml` 整份重写。原来是透明背景 + ListView 贴课表位图，改为玻璃卡 + 每天一行（`wrow_0` ~ `wrow_6`，周一至周日）。
- 新增 `WidgetData.getWeekPlan`，汇总每天的课程名。
- 删除 `ScheduleAppWidgetService.kt`、`item_schedule_widget.xml` 及其在 manifest 里的注册。
- `refreshScheduleWidget` 去掉适配器，改为按卡片高度自适应 1 ~ 7 行。

## v107 / 3.638 · 修掉今日卡「加载中…」的真根因

这是这个项目里最值得记一笔的一个 bug。

- **根因**：今日卡没有配置页（info xml 里没有 `android:configure`），所以它从未被登记进 `appwidgetbean` 表。而当时所有刷新入口都在遍历数据库里的登记记录，循环恒为空，等于谁都没刷新过它。
  周课表卡之所以正常，是因为它有配置页，会被写进登记表。
- **修法**：刷新不再依赖数据库登记，改用系统传入的 `appWidgetIds`；广播路径用 `ComponentName` + `getAppWidgetIds` 拿到全部实例并自动补登记。
- 顺带把智能预告从「只看今明两天」扩展为「往后找最近有课的一天」。

## v108 / 3.639 · 预览图重画与尺寸调整

- 三个小部件的 `previewImage` 全部重画。原本今日卡用的是上游项目自带的蓝色旧截图，另两个没配预览图，被系统兜底成白底小图标。
- 尺寸调整：今日卡 150 → 240dp，周课表 190 → 200dp，下一节课 110 → 140dp。

## v109 / 3.640 · 加课入口与颜色对齐

- 周课表小部件右上角新增「+」，点击直达加课页。
- 新建同名课程时，颜色自动沿用已有同名课程的颜色，避免同一门课两个色。

## v110 / 3.641 · 课程预告范围设置

- 新增「课程预告范围」二选一：「提前 2 天」/「提前 7 天」。用单值存储，两个选项天然互斥。
- 空态短句库 55 条，卡片无课时随机展示。

## v111 / 3.642 · 预告语义修正与短句库扩充

- 「预告范围」的语义修正为「从今天起共 N 天」。提前 2 天 = 今天 + 明天。
- 空态短句库扩到 255 条：幽默 66 条、励志 79 条、关心 70 条、遗憾 40 条，全部不超过 10 个字，资源体积约 5.8KB。

## v112 / 3.643 · 生日提醒板块

- 今日卡底部新增生日提醒板块，**只在今天课程全部上完时**才出现（今天本来没课也算）。
- 日期选择用 `NumberPicker` 滚轮，只选月和日；年份由 `BirthdayUtils.nextYear` 推断：月日已过算明年，未到算今年，正好是今天算今年。
- 新增文件：`BirthdayUtils.kt`、`BirthdayReminderActivity.kt`、`activity_birthday_reminder.xml`、`widget_birthday_bg.xml` / `widget_birthday_bg_dark.xml`。
- 偏好键：`birthday_month` / `birthday_day` / `birthday_text`。
- 入口：课表设置 → 桌面小部件外观 → 「生日提醒」。
- 板块出现时，课程行压缩到最多 2 行，避免底部被裁。

## v113 / 3.644 · 课表网格内拖拽移动课程

本轮最大的一次交互改造。

- 长按课程块起拖，拖动过程中实时高亮目标格，松手落位。
- 一次移动**整组**：同名 + 同节次 + 同 step 的多条周次分段（单双周、散周）一起走，保留 step 与周次不变。
- 落位后弹出 Snackbar 提供「撤销」，可以直接搬回原位。
- 数据结构上先按旧复合主键删除、再插新记录（`@Transaction` 包住）。`CourseDetailBean` 的主键是 `(day, startNode, startWeek, type, tableId, id)`，改位置就换了主键，直接用 `@Update` 会更新 0 行，用 `REPLACE` 会残留旧记录。
- 新增文件：`CourseDragController.kt`、`drawable/course_drag_highlight.xml`；`ScheduleFragment.kt` 增加长按回调与 `onCourseMoved`；`CourseDao.kt` 增加 `moveCourseDetails` 事务方法。
- 只支持移动位置，不支持拉长 / 缩短课程时长。

## v114 / 3.645 · 课表同步到系统日历

起因是想在 ColorOS 的负一屏（速览）上看到今天的课。

桌面小部件和负一屏是**两套完全独立的体系**：桌面走标准 Android `AppWidget`，负一屏走 OPPO 自研的速览卡片（`CardWidget`），后者需要 OPPO 开放平台的开发者认证与商务审批，短期接不上。

于是先做一条立刻能用的路：把整学期课表写进系统日历。负一屏和桌面上都有「日历」类卡片，它们读的是系统日历数据，写完就能显示。

- 入口：课表页 → 导出 → **「同步到系统日历」**。
- 首次同步会申请日历读写权限，然后让你挑一个可写的日历（只有一个时不弹框，直接写）。选择会记住，下次不用再挑。
- 课程转成**每周循环的日程**：连续周用 `FREQ=WEEKLY;COUNT=n`，单双周用 `FREQ=WEEKLY;INTERVAL=2;COUNT=n`。一门课通常只占 1 条日程，不会把日历塞满。
- 每节课提前 15 分钟提醒。
- **可反复同步**：每条日程的备注第一行带 `[小唐Tangle#课表id]` 标记，同步前先按标记把上一次写进去的整批删掉，所以改完课表再点一次就行，不会堆重复，也不会误删别的课表的日程。
- 新增文件：`CalendarSyncUtils.kt`；`ExportSettingsFragment.kt` 与 `fragment_export_settings.xml` 加入口；manifest 加 `READ_CALENDAR` / `WRITE_CALENDAR`。
- 权限申请的 `requestCode` 为 `Const.REQUEST_CODE_CALENDAR_PERMISSION`（109）。

## v115 / 3.646 · 今日卡两种排列（竖排 / 紧凑两列）

起因是两件事：竖排卡课少的时候下面空一大片，卡片本身又占地方；而且课程行是全宽的，信息全挤在左边，右边一大片空白。

于是给今日卡加了第二种排列，两种随时切换。

**排列一 · 竖排列表（原样式，右侧补上时间）**

- 每行仍然是全宽一门课，但信息重新分了区：左边是课名 + 地点（没地点退到老师，都没有就显示节次），右边是开始时间（加粗）+ 结束时间。
- 右边那片空白就这么填上了，一眼能看出「几点上课」。
- 正在上的课，右下角把结束时间换成「正在上」小胶囊。

**排列二 · 紧凑两列（新样式）**

- 课块变成半宽，左上 + 右上并排，一行两门课，一格只占两格宽。
- 格内两行：课程名 + 「10:10-12:00 @A1-414」。
- 同一张卡的高度，从显示 4 门课变成最多 6 门（3 行 × 2 格）。
- 卡片最小高度从 240dp 降到 120dp，能压到两格高。
- 右边的空格子用 `INVISIBLE` 占位而不是 `GONE`，否则同一行左边那格会被 weight 撑成整行宽。

**递补（两种排列共用）**

- 卡片永远只显示「还没上的前 N 门课」：已上完的课在上课结束那一刻被精确闹钟顶掉，后面的课自动递补上来，多出来的不显示。
- 当天课全上完（或本来就没课）→ 卡片自动跳去预告下一个有课的日子（智能模式）。

**入口**：课表设置 → 桌面小部件外观 → 「今日卡排列」，二选一，选完立刻刷新卡片。

- 新增偏好键 `Const.KEY_TODAY_CARD_LAYOUT`（0 = 竖排，1 = 紧凑两列）。
- 新增控件：竖排行 `row_time_N` / `row_end_N`；紧凑网格 `ll_grid` / `grow_0`~`grow_2` / `gcell_0`~`gcell_5`（含 bg / bar / name / info）。

---

## v116 / 3.647 · 三处装机反馈修正 + 补回丢失的生日板块

这一版全部来自装机后的反馈截图。

### 1. 今日卡「明明还有位置，却只显示一门课」

对着截图量了像素：两格高的卡片实际渲染高度约 158dp，里面一行课程块占 50dp，课程块下方还剩约 39dp 的空白。

原来的行高 50dp、行距 6dp、卡片内边距 12dp、标题区固定预留 40dp，算下来一行就把可用空间占满，剩下 39dp 又不够再放一行（要 56dp）；而算行数的公式里还留着 58dp 的固定预留，158dp 只算出 1 行。所以不是数据缺失，是行数公式算得太紧。

- 行高 50dp 压到 44dp，行距 6dp 压到 4dp，卡片内边距 12dp 压到 10dp，列表上间距 8dp 压到 4dp。
- 公式重写为「一行 104dp，每多一行加 48dp」，并且比实际需求再宽松 6dp，宁可多给一行也不要白留空白。
- 高度取值改为 `OPTION_APPWIDGET_MIN_HEIGHT` 优先。竖屏下它才是卡片当前高度，`MAX_HEIGHT` 是横屏时的值，原来的代码先取 `MAX_HEIGHT`，语义是反的。
- 结果：两格（约 158dp）能显示 2 门课，三格 3 门，四格 4 门；紧凑两列同一高度能显示 4 格。

### 2. 生日提醒板块从来没出现过（补回丢失的渲染）

v112 说要加「生日提醒板块」，但回头看代码：布局 `ll_birthday`、drawable `widget_birthday_bg`、设置入口、`BirthdayUtils` 全都在，唯独 `refreshTodayWidget` 里渲染它的那段代码一直缺失（只剩一个没人引用的 import）。`ll_birthday` 的可见性从来没人设置，所以永远是布局里的 `GONE`，功能等于没上线。

- 补上渲染。出现条件四个同时满足：不是手动翻看别的日子、已经设过生日、今天已经没有未上的课、生日在 30 天以内。
- 「今天课全上完」不能只看 `offset == 0`，智能模式下这时卡片已经跳到明天了，必须单独拿今天的课判断。这条是 v112 就记下的坑，这次实现时踩实了。
- 板块连间距约要 103dp：空间不够就让课程行逐行让位，连一行课程都保不住时干脆不显示板块。所以两格卡片看不到它，这是有意的，不去挤压课程。
- 只在生日临近（30 天内）才显示，不然一年到头天天占着卡片底部。
- 右侧显示「还有 N 天」，当天显示「就在今天」；点整个板块进设置页改日期或寄语。

### 3. 设置页「今日卡排列」等三行没有图标

`SettingIcons` 是一张「条目标题 → 图标 + 彩色圆角块」的映射表，没登记的标题返回 null，那一行左侧就空着。新增设置项时忘了补映射。

- 补上三条：今日卡排列（三横线）、课程预告范围（日历）、生日提醒（铃铛）。

### 4. 默认每节课时长 50 分钟改成 45 分钟

- `TimeTableBean.courseLen` 默认值 50 改成 45。
- 三处默认时间段列表统一改成 45 分钟版（`TimeTableDao.initTimeTable`、`TimeSettingsViewModel.initTimeTableData`，以及 `UpdateUtils` 里两处首装初始化）：开始时间不动，结束时间改成开始 + 45，即 08:00-08:45、09:00-09:45、10:10-10:55 ……
- 数据库迁移里那条 `ADD COLUMN courseLen INTEGER NOT NULL DEFAULT 50` 保持原样。那是历史迁移语句，改它会污染老用户的升级路径，新建表的默认值由实体类决定，已经够了。

---

## v117 / 3.648 · 长课名不再把教室名挤出格子

装机反馈：9 月 15 日那门「微机原理与单片机接口技术」，课名太长，在格子里竖排占满 6 行，把最后一行「@A1-504」顶到格子外，只露出半行 `@A1`。

**根因**：格子里的文字被拼成一整串交给 `TipTextView` 自绘，用 `StaticLayout` 一次排完，再 `canvas.clipRect` 裁到格子范围内。排几行完全由课名长度决定，超出的行连同一个「…」都没有就被直接裁掉，而裁掉的正好是排在最下面的教室名。

**改法**：不再把文字当一整串，按重要性拆成四段交给 View 自己排：

```
课名  >  教室  >  开始时间  >  单双周
```

- 先给「教室」这一行留好位置（它是最容易被长课名挤掉的信息）；
- 课名按剩余高度做**限行 + 省略号**，而不是任其换行把后面的行顶出去；
- 只有连「课名 1 行 + 教室」都放不下的极端情况（格子特别矮或文字特别大），才依次丢掉「开始时间」和「单双周」。

所以那门课现在显示为 5 行课名（末行带「…」）+ 完整的一行「@A1-504」。

- 排版放在 `TipTextView` 内部而不是拼字符串时截断：格子高度随「课程格子高度」设置、节次（step）、文字大小三处变化，只有拿到实际宽高才能算出能排几行。
- 省略号用逐次回退试排（`makeEllipsizedLayout`），因为 API 21 没有 `setMaxLines` / `setEllipsize`，而中英文混排下按字符数估算宽度不靠谱。
- 顺带修掉两处隐患：`CourseBean.room` 是可空类型，原来直接拼进字符串会出现字面的 `@null`；`[非本周]` 在没有单双周时原本会贴在上一行末尾，现在独立成行。
- 文字大小、格子高度、是否显示开始时间等设置照旧生效，只是都改由新排版算法消费。

## v118 / 3.649 · APK 固定签名（可覆盖安装）

这一版没有任何界面改动，改的是构建方式。目的是回答一个很实际的问题：**能不能直接拿新 APK 盖在旧版本上装，而不用先卸载、导数据？**

**先说结论：Android 允许覆盖安装，但要同时满足三个条件。**

| 条件 | 本项目情况 |
| --- | --- |
| 包名相同 | `com.Tangle.timetable`，一直没变 |
| 签名证书相同 | 逐版核验，v114 ~ v117 的证书 SHA1 完全一致 |
| `versionCode` 不降级 | 每版 +1，当前 118 |

只要有一条不满足，安装器就只允许「先卸载再装」，而卸载会连应用数据一起清掉——课表、设置全丢。这就是之前每次升级都要先导出的原因。

**隐患在哪**：之前用的是 Android 调试签名，密钥是构建机用户目录下的 `~/.android/debug.keystore`，由 IDE / 首次构建自动生成，属于「谁也不知道它在哪、随时可能没」的东西。清理工具会删它，换台机器编译更是会生成一份全新的，证书一换，覆盖安装立刻失效。

**改法**：把密钥固定进项目。

- 把当前生效的调试密钥复制一份到项目内 `keystore/xiaotang.jks`，并在项目外另存一份备份；
- `app/build.gradle` 里新增条件签名配置：密钥文件存在就用它签，不存在就退回系统默认调试签名，所以别人 clone 下来照样能编译；
- `keystore/` 已加入 `.gitignore`，私钥不进公开仓库。

之后无论重装系统、换构建机、还是清理工具扫过一遍，只要项目里的密钥在，产出的 APK 就永远同签名，可以一直覆盖升级。

**验证**：v118 编译后用 `keytool -printcert -jarfile` 比对，证书 SHA1 与 v117 完全一致（`4E:FA:65:D0:…:4F:B4`），三项前提逐条核实通过。

> 一个例外：如果手机上装的是更早时期（v104 ~ v113）的版本，那些 APK 已经不在手上了，无法核验它们的证书是否同源。若首次换到 v118 时提示「应用签名不一致」，卸载一次即可，之后每一版都能顺畅覆盖。

---

## v119 / 3.650 · 修掉「设置生日时莫名跳到选择时间表」

装机反馈：在课表设置里进「生日提醒」拨好日期，保存返回后界面会自己跳到「选择时间表」（时间设置里那个列着 08:00、10:10 等多套时间表的列表）。不是每次都跳，但经常跳。

**根因不在生日页，在课表设置页的「快捷入口」机制。**

课表页上的「上课时间 / 当前周 / 课程表背景」三个按钮，是靠给设置页传一个 `settingItem` 参数实现的（如 `settingItem = "上课时间"`）：设置页收到后延时自动滚到并展开对应那一项。对「上课时间」来说，展开就等于启动时间设置页，也就是「选择时间表」。

问题出在两处：

- 这个 `settingItem` 参数只用一次却不消费。设置页进了子页面（比如生日提醒）之后，一旦被系统回收，返回时重建，原 intent 里的参数还在，自动展开就又执行了一遍；
- 自动展开没有区分「全新打开」和「恢复现场」。后者本就不该再执行跳转。

于是用户看到的现象是：拨完生日滚轮保存、返回设置页的一瞬间又被带去「选择时间表」，看起来像「设置生日导致的跳转」，实际是设置页的旧快捷指令在重放。

**修法**（`ScheduleSettingsActivity.onCreate`）：

- 读到 `settingItem` 后立刻 `intent.removeExtra("settingItem")` 消费掉，重放没有素材；
- 只在 `savedInstanceState == null`（全新创建）时执行自动展开；恢复现场（重建）只恢复界面，不再跳；
- 顺带把 `as LinearLayoutManager` 改成安全转换 `as?`，布局管理器类型不符时不再抛异常。

**生日输入逻辑本身不需要改**：生日页就是两个滚轮，左轮「1月 ~ 12月」，右轮「1日 ~ 31日」，天数跟着月份联动（2 月自动按闰年给 28 / 29）。要设 7 月 1 日，把两个轮子拨到「7月」「1日」保存即可；页面下方会实时预览「将于 ××××年7月1日 提醒你 · 还有 N 天」。系统只记月日、不记年份：今年这天过了就算明年，没到就算今年。

---

## v120 / 3.651 · 修掉「茅台学院导入不全」：自动展开完整课表再抓取

装机反馈：在茅台学院教务的「我的课表」页直接导入，课表不全；必须点进页面右上角的「更多」，在展开后的完整课表里再导入，数据才齐全。

**根因在页面本身：那个视图只渲染当前一周的课程，跟解析器无关。**

「我的课表」是**按周渲染**的视图（顶部带「‹ 第 2 周 ›」和「回到当前周」）：页面 DOM 里只存在**当前教学周**的课程块。那些只在别的周才上的课（如「大学英语（一）3-3 周」「理论仿真实验 13-16 周」「电力电子技术 6-6 周」）在第 2 周的 DOM 里根本不存在，抓取工具自然抓不到。

点「更多」进入的是**整学期视图**（带「起止周 / 双节次」筛选和导出打印），DOM 里是全部课程，所以在那里导入就是完整的。

佐证很直接：周视图的课程卡片里**没有周次行**（只有「星期一 3,4 节」这类），而完整视图的卡片每张都标着周次（「4-5;7-16 周」）。两种卡片的字段结构本就不一样，前者连周次文本都不存在。

**修法**（`WebViewLoginFragment`）：导入按钮不再直接抓源码，改走「智能抓取」三步 ——

1. 先注入探测脚本：若当前页像课表页（含 `courseBox` 或"星期 / 节"字样），就找一个文本为「更多 / 更多» / 全部课表 / 完整课表」的**可见可点击**元素点一下（优先 `<a>`/`<button>`、子元素少的节点，即最接近真正按钮的那个）；
2. 点击后等待展开 / 跳转完成再抓源码：若点击触发了页面跳转，在 `onPageFinished` 里接着抓；另外挂了 3.5 秒兜底（同页展开场景）与 6 秒兜底（页面还在加载的场景），且用 `expandTried` 标记防止跳转后重复点「更多」造成死循环；
3. 找不到「更多」、或页面不是课表页，则**退化为旧版行为**（直接抓当前页），不影响其他学校。

配套两处增强：

- **每次导入都把抓取到的页面存一份留档**：Android 10+ 写进公共 Download（`wakeup_import_ok_*.html` / `wakeup_import_empty_*.html`，MediaStore，免权限），文件开头附一段诊断注释：解析出几门课、课程列表与每门课的明细。以后再遇到"导入不全"，把该文件发来即可对照「页面里有什么」与「解析出了什么」，不用反复截图。
- 抓 iframe / frame 内容时补上 `try/catch`：跨域 iframe 访问会抛异常，旧写法一旦遇到就会中断整个抓取脚本（表现为点了导入没有任何反应）。

---

## v121 / 3.652 · 加「每日课表」：每天一条整天汇总

装机反馈：走「同步到系统日历」这条路之后，负一屏上能看到课表了，但只有「一屏一周」的周课表形态，缺「这一天要上什么」的每日形态。

**原因不在数据，在展示粒度**：此前同步进日历的只有「每节课」的循环日程，一周排开自然就是一张周课表；而「某一天一共几节课、都是什么」这个粒度的信息，日历里没有。

**修法**：同步时再加一类日程 ——「每日课表」。

- 每个有课的星期几各写一条**全天日程**，标题「今日课表 · N 节」；
- 备注里列出当天每节课的「时间 课名 @教室」，例如：

  ```
  08:20-10:00 大学英语（一）@A1-306
  10:20-12:00 自动控制理论@A1-324
  ```

- 覆盖的周次按「该天实际有课的周次」合并计算（单双周、散周都对），连续段用 `RRULE` 循环；
- 全天日程不带提醒（每节课的「提前 15 分钟」提醒原样保留）；标记用 `[小唐Tangle#日课#课表id]`，与每节课的 `[小唐Tangle#课表id]` 分开管理，同步时一起先删后插，照旧幂等。

这样系统日历按天看（日历 App 的日 / 月视图、负一屏的「日程」卡片、日历桌面小部件）每天都能一眼看到「今天几节课」，和一屏一周的周课表正好互补。

**实现里踩到的一条硬规范**：全天日程必须用 **UTC 时区 + 午夜边界**（`ALL_DAY=1` + `EVENT_TIMEZONE=UTC` + 该日期的 UTC 零点），否则部分日历会把日期显示错位一天。见技术备忘第 21 条。

---

## v122 / 3.653 · 加「图片导入」：识别课表截图变成课表

需求原话是「课表导入功能不够完美，需要一个通过导入图片，然后导入课表的功能」。

**方案**：本地离线 OCR + 结构化还原，全程不联网、不传图。

- OCR 用 **Tesseract 5 + 简体中文训练数据**（`chi_sim`，2.4MB，随 APK 打包在 `assets/tessdata/`，首次使用时释放到应用私有目录）。用「稀疏文本」模式识别，适合课表这种文字散落在格子里的排版。
- 识别出来的不只是文字，还有每个词的坐标。`ImageTableRecognizer` 靠坐标还原结构：
  1. 找日期表头（`周一` ~ `周日` / `星期一`），它们的横向位置就是 7 列的锚点；
  2. 找左侧节次列（`1` ~ `12` / `第1节`），纵向位置是每节课的行锚点，用最小二乘拟合行高（个别错识别不会带偏整条轴）；
  3. 其余文字按「最近的列 + 纵向相邻」聚成一个个课程块；
  4. 块内按行拆文本，分出课名 / 教室 / 周次（`1-16周`、`3-15周(双)` 这类都能解析）；
  5. 块的纵向跨度决定它占第几节到第几节。
- **找不到表头不会直接失败**：没有日期表头就按宽度等分 7 列，没有节次列就按高度等分，并在提示里说明「已按等分推算，可能有偏差」，让用户自己判断结果可不可信。
- 识别结果先列出来给用户核对（`1. 高等数学  周一 第1-2节  1-16周  A1-414`），确认无误再写库，避免自动写错数据。
- 入口：课表页 → 导入 → 「从图片导入（识别课表截图）」。
- 图片先采样到最长边 1800px 以内再识别，兼顾精度与速度。

**踩到的一堵硬墙**：官方 OCR 库（Tesseract4Android 4.8.0）的 AAR 里，class 文件是 Java 17 字节码（major 61），而本项目用的 AGP 3.5.3（2019 年）只认到 Java 8，Jetifier 转换该 AAR 时直接崩溃（`Failed to transform ... using Jetifier. Reason: null`），加 `android.jetifier.blacklist` 也绕不过（失败点在 processed-jar 阶段，不在 blacklist 作用范围）。

**解法**：该库源码本身只用 Java 8 级 API，于是**用 JDK 8 重编译它的源码**得到 major 52 的 class，打成 `app/libs/tesseract4android-4.8.0-jdk8.jar`；native 库（`.so`）原样取自官方 AAR，放 `app/src/main/jniLibs/`。详见技术备忘第 22 条。

---

## v123 / 3.654 · 修掉「拨生日滚轮时整页退出到课表设置」

装机反馈原话：「在生日时间设置那里，跳转到别的地方的 bug 还没修复。拨动日表格并不会触发跳转，但是拨动月表格，只要超过两个月以上，它就会直接跳转到课表设置。」

**先说清楚为什么 v119 没解决**——两次是同一种症状，但机制完全不同：

- **v119 修的是「快捷指令重放」**：课表设置页的 `settingItem` extra 未被消费，页面重建时又执行一遍自动展开，把用户带去「选择时间表」。触发条件是**页面被回收重建**，与手指动作无关。
- **本次是「系统边缘返回手势抢走了竖滑」**：与页面重建无关，与手指从哪起手、横向漂移多少有关。v119 的两个守卫（`removeExtra` + `savedInstanceState == null`）对它完全无效。

**定位过程与依据**：

1. 通读生日页及其基类，**代码里不存在任何一条通往课表设置的导航路径**。生日页是父页用 `startActivityForResult` 拉起的，所以能落到课表设置的原因只剩一个：**本页被 `finish()` 了**。全页只有两处会 finish —— 保存按钮，和返回键。
2. 用户描述里的关键不对称：**「日」表盘不触发，「月」表盘触发**。而布局里两个滚轮是左右并排的：`np_month` 在左、`np_day` 在右。
3. 于是指向 Android 10+ 的**边缘返回手势**：手指从左缘附近起手竖滑拨「月」滚轮时，只要横向漂移够大，系统会把这一串判成「左侧返回」，本页随即退出。「日」滚轮在右半边、离边缘远，所以拨它没事。这一条同时解释了「为什么是左轮」和「为什么拨得越多越容易中」（漂移更大）。

**修法**：

- 给滚轮那一行加 id（`ll_pickers`），在`BirthdayReminderActivity` 里用 `setSystemGestureExclusionRects()`（API 29+）把**这排滚轮所在的整条横向区域**从系统手势判定区里排除掉；矩形左右**都拓到屏幕边缘**，这样手指从左缘起手也落在排除区内。`post {}` + `addOnLayoutChangeListener` 保证键盘弹收、布局变化后矩形仍然贴着滚轮。
- 给滚轮行加 `paddingStart/End = 16dp`，让滚轮离屏幕左右缘更远。这是给 **API < 29**（没有 exclusion API）的老系统留的兜底。
- 原有的「要回滚动手势控制权」（`requestDisallowInterceptTouchEvent`）保留，它管的是外层 `ScrollView` 抢手势，与本次是两个不同的问题。

**技术备忘第 24 条**记了这个坑的通用形态：**横向排布的滚动控件贴着屏幕边缘时，会和系统边缘返回手势打架。**

---

## v124~v129 / 3.655~3.660 · 端到端代码审查收口（随 v129 一次性发布）

对全部 187 个 Kotlin 文件做了一轮端到端审查，五批修复一次收口，随 v129/3.660 发布：

- **稳定性**：课表文件导入加固、跨天时间计算丢分钟、课表页监听器/Observer 累积泄漏、添加课程空指针、空列表越界、周数越界钳制；
- **小部件**：刷新改后台线程杜绝 ANR、多课表切换后周小部件串表、倒计时「明天」误判、空色回退统一；
- **安全**：WebView SSL 错误不再静默放行、登录页 Intent 参数校验、全局 PendingIntent 补 FLAG_IMMUTABLE；
- **兼容**：茅台学院教务周日列漏解析修复。

细节见仓库内《小唐Tangle_开发者变更日志_v129.md》。

---

## v130 / 3.661 · 修掉「负一屏课程卡没有课」：日历同步适配新版 ColorOS 私有日历库

装机反馈：负一屏的课程卡（数据源是系统日历）一直显示「暂无课程安排」，点「同步到系统日历」直接弹 **「手机里没有找到可以写入的日历」**。

**根因**：OPPO 官方文档明确，新版 ColorOS 日历（versionCode ≥ 7001000，一加13 属于此类）把日历数据整体搬进了**私有 Provider，AUTHORITY 是 `com.coloros.calendar`**，原生的 `com.android.calendar` 基本只剩 exchange 同步数据。而 v114 的同步代码写死了走 `CalendarContract`（原生库），于是在新机型上：

1. 查日历列表 → 原生库是空的 → 报「没有找到可以写入的日历」；
2. 一条日程都没写进去 → 日历和负一屏自然全空。

**修法**：

- `CalendarSyncUtils` 改为**多 Provider 探测**：按 `com.coloros.calendar` → `com.oplus.calendar` → `com.oneplus.calendar` → 原生 的顺序逐个查，哪个库里有可写日历就用哪个，读写（查列表 / 删旧日程 / 写日程 / 写提醒）全程走同一个库，不再写死 `CalendarContract.AUTHORITY`；
- `SyncCalendar` 携带所属 authority，选中的日历在哪个库，同步就写进哪个库；
- 严格条件（可写 + 可见）查不到时放宽条件再扫一遍，兼容个别 ROM 把本地日历标记得很低的情况；
- 查不到日历时的报错文案改为可行动的提示：若在 ColorOS 上把本应用日历权限设成了「仅允许创建」，系统会在 Provider 层拦截查询，需要到 系统设置 → 应用 → 小唐Tangle → 日历权限 改成「允许全部」。

**技术备忘第 25 条**记了这个坑的通用形态：**国产 ROM 的"系统 Provider"可能被厂商整个换掉 AUTHORITY，写死 `CalendarContract.AUTHORITY` 在新机型上等于查了个空库。**

---

## v131 / 3.662 · 日历同步自动建日历 + 课名/文字排版修复

**一、同步日历时自动创建本地日历**

v130 让同步能找到 ColorOS 私有日历库了，但流程上仍然假定「手机里本来就有一个可写入的日历」：一个都查不到就直接报错退出，用户永远同步不了。这一版补上兜底：

- 新增 `CalendarSyncUtils.ensureLocalCalendar()`：查不到任何可写日历时，用 sync-adapter 身份（`CALLER_IS_SYNCADAPTER=true` + `ACCOUNT_TYPE_LOCAL`）**插入一个属于本应用的本地日历「小唐Tangle课表」**（主色 iOS 蓝、可见、可写），随后所有读写都走它；
- 按 `com.coloros.calendar` → `com.oplus.calendar` → `com.oneplus.calendar` → 原生 的候选顺序依次尝试创建，哪个 Provider 建得成用哪个；
- 每次同步都重新探测/复用（按显示名找已建的那一个），不依赖用户上次选过哪个日历，**第二次、第三次同步都能正常跑**；
- 只有连创建也失败（例如 ColorOS 日历权限被设成「仅允许创建」，Provider 层把插入也拦了）时，才提示去系统设置改权限。

**二、课名不再把教室名顶出格子（真因）**

v117 已经做了「课名限行 + 教室优先」，但**教室行的占位是固定按 1 行算的**。教室名稍长（例如 `@A1-505` 之外的 `@教学楼A区301` 这类）自己会换行成 2 行，课名仍按「总数 - 1」去排，于是尾部整体超出格子、被 `clipRect` 裁掉——表现就是「教室名被遮住/看不见」。

修法：改成**先按实际排版出来的行数**测出「开始时间 / 教室 / 单双周」各占几行，剩下多少行才给课名；同时给课名上限 **4 行**（超出加省略号），避免超长课名把格子排成一堵墙。

**三、图片识别结果列表排版**

识别结果原来把「序号 + 课名 + 星期 + 节次 + 周次 + 教室」全塞一行，课名一长整行就被撑爆、后半段信息被挤没。改为每门课两行：

```
1. 微机原理与单片机接…
      周一 第1-2节  1-16周  @A1-505
```

课名超过 14 字自动截断加省略号（预览够用了，真正写库用的是完整名字）。

---

## v132 / 3.663 · 第二轮全量代码审查修复（20+ 项）

对应《code_review_report_v131.md》。逐项根因分析与位置见报告，此处列用户可感知的变化：

**稳定性**
- 小部件统一刷新接收器真正接入后台执行（v126 的修复只改了一半，本版补齐）—— 亮屏、每节课闹钟、开机等高频路径不再在主线程查库
- 课表页在「节数 > 时间段数」设置下不再崩溃；课程详情时间取值加越界保护
- 周课表部件遇到脏的绑定信息不再崩进程（改用与统一刷新一致的容错口径）
- 首装无默认课表时进课表设置不再 NPE 闪退（友好提示后退出）
- 西工大导入遇到无括号后缀课程名不再失败；所有解析器的崩溃统一转成可读的导入失败提示

**多课表正确性**
- 切换默认课表后，课表网格立即渲染新表课程（原来会一直显示旧表数据）
- 增删改课后，绑定非默认表的周课表部件按各自绑定的课表刷新（原来被刷成默认表）
- 新建课表的开学日期默认为本周周一（原来是 2020-02-03，导致周次恒错位）

**桌面小部件**
- 移除可被第三方应用伪造「上课提醒」通知的死通道
- 课程闹钟每次重排前先清理旧闹钟：关闭课前提醒 / 删课 / 缩课后不会再弹残留通知
- 「下一节课」倒计时：今天还有未开始的课不再显示「明天」
- 周概览跨年不再显示「13月」
- 改课后的小部件刷新广播显式限定本应用（Android 8+ 隐式广播被拒收导致的「改课不刷新」）

**日历同步**
- 记忆的日历改为「库 + id」组合键，多 Provider 场景不会再写错日历
- 每次同步 / 删除课表时按所有候选日历库全量清理旧日程（换库不再残留）
- 删除课表增加确认弹窗，并连带清理该表的日历日程与小部件绑定

**其他**
- 「覆盖同名课」改为真覆盖（原来新旧时间段叠加、列表出两条同名课）
- 编辑课程保存后管理页列表立即刷新
- 清空课表后同步刷新桌面小部件
- 导出文件 / ICS 失败会弹出错误提示（原来完全静默）
- 主题监听器通知改为快照遍历，杜绝主题切换时 ConcurrentModificationException
- WebView 导入的 JS 桥增加站点校验：非教务站点的页面无法静默触发导入

**已知取舍 / 需在线验证（本版未动）**
- 苏州大学 / 华科 / 吉大导入仍走 HTTP 明文（改 https 需逐校在线验证，盲改可能导致导入不可用）
- release 签名密码随上游 WakeUp 仓库公开的历史遗留（更换签名会导致无法覆盖安装，需单独决策）
- Gson 2.8.6 CVE 可升 2.8.9，但离线环境无法验证新包可用性，未动

---

## 技术备忘（踩过的坑）

以下每一条都是实际踩过并修掉的，留给后来者。

1. **今日卡一直「加载中…」有两个完全不同的根因**，排查时两个都要看：
   - 刷新入口只遍历数据库登记表，而今日卡没有配置页所以从未被登记（v107 修）。
   - 行内容用 Bitmap 下发，超过 RemoteViews 约 1MB 的传输上限，整次更新被丢弃（v105 修）。
   两者的症状都是「停在布局文件默认文案」，很容易混淆。

2. **RemoteViews 逐行着色**只能走 `ImageView.setColorFilter`。ImageView 用白色圆角 drawable 作 src，再 `setInt(id, "setColorFilter", 颜色)`。直接 `setBackgroundColor` 会丢圆角。

3. **RemoteViews 不支持 `setAlpha`**，也做不了真正的背景模糊（毛玻璃），只能半透明白 + 细描边近似。

4. **批量引用 id 用显式数组**，不要用 `R.id.xxx_0 + i` 这种算术递增。

5. 卡片显示几行，按 `AppWidgetManager.getAppWidgetOptions()` 拿到的实际高度算，避免滚动条与留白。

6. `CourseUtils.countWeek` 不会把 `startDate` 吸附到周边界，`countWeek = daysBetween / 7 + 1`。`startDate` 必须填该周第一天，否则周次会差 1。

7. **ConstraintLayout 里动态定位不能用 `setX` / `setY`**，会被约束布局覆盖。要用 `LayoutParams.marginStart` / `topMargin` 配 `startToStart` / `topToTop`。

8. **`DragEvent` 的坐标基准是「接收该事件的 View」**，分发细节不可依赖。拖拽时加了一层与网格同原点的透明接收层（`dropZone`）把坐标原点钉死。

9. 拖拽用 `View.startDrag`（API 11+）而不是 `startDragAndDrop`（API 24+），以保住 minSdk 21。

10. **循环日程必须给 `DURATION`，不能给 `DTEND`。** CalendarContract 规定循环事件（带 `RRULE`）里的 `DTEND` 会被忽略，要用 `DURATION`，格式为 RFC2445 的时长写法（如 `PT1H30M`，注意 `T` 不能省）。只有「只上一次」的单次日程才走 `DTEND`。

11. **算「哪几周要上课」不能直接用 `startWeek..endWeek` 配固定步长。** 单双周课程的奇偶性与 `startWeek` 不一定对齐，直接按 2 步长循环会整体错位。正确做法是先用 `CourseBean.inWeek(w)` 把真实要上课的周次过滤出来，再从结果列表推导步长与次数。

12. **反复同步不能靠「记得先删」。** 把标记写进日程的 `DESCRIPTION`（这里是 `[小唐Tangle#课表id]`），同步前用 `DESCRIPTION LIKE '[标记]%'` 整批删掉。标记里带上课表 id，多张课表就互不干扰。

13. **DialogFragment 里要先弹结果再 `dismiss()`。** Fragment 一旦 dismiss，`lifecycleScope` 上的协程会被取消，后面的代码就不执行了。要保证结果对话框在 dismiss 之前弹出来，或者干脆用 Activity 做 context 弹（Activity 级对话框不受 Fragment 生命周期影响）。

14. **Fragment 里申请运行时权限要用 `requestPermissions()` 并重写 Fragment 的 `onRequestPermissionsResult`。** 如果图省事写成 `ActivityCompat.requestPermissions(activity, ...)`，回调只会送到 Activity，Fragment 收不到，整个流程就断在那里。


15. **同一行里的半宽格子，空位要用 `INVISIBLE` 而不是 `GONE`。** 用 `layout_weight` 平分宽度时，把右格设成 `GONE` 会让左格吃掉整行宽度，「半宽」就没了。`INVISIBLE` 不绘制但保留占位。整行都没课的情况另外用行容器收掉（`GONE`），免得白白多出 6dp 行距。

16. **「上完的课自动消失、后面的课递补」靠的是每节课结束的精确闹钟，不是刷新周期。** `WidgetScheduler.scheduleCourseAlarms` 会给当天每节课的 `endMillis` 注册 `setExactAndAllowWhileIdle`，到点触发 `refreshAllWidgets`。想改「已结束的课怎么显示」，改的是 `WidgetData` 里那道过滤条件，不用碰刷新时机。

17. **往固定高度的格子里塞文字，要「保尾部、裁高优先级文本」，不能一把交给 `StaticLayout` 排完再裁。** 格子里的信息重要性不一样（课名 > 教室 > 时间 > 单双周），一次性排版等于默认「谁排最后谁先被裁」，长课名必然把教室名挤出去，而且连省略号都不会有。正确做法是按重要性分段，先给关键行占好位，再对可牺牲的文本做限行 + 省略号。API 21 上 `StaticLayout` 没有 `setMaxLines` / `setEllipsize`，用「先整串排一次拿到行尾索引，再截断重建」的循环回退最稳，也能顺便绕开中英文混排的宽度估算问题。

18. **想长期「覆盖安装不丢数据」，就必须把签名密钥当成项目资产管起来。** 覆盖安装的三个条件是包名、签名、`versionCode` 递增，其中签名最容易被忽略：调试构建默认用 `~/.android/debug.keystore`，这个文件由工具自动生成、可被清理、换机器就换一份，一旦变动，用户只能卸载重装，数据全丢。做法是把密钥复制进项目、`.gitignore` 排除、`build.gradle` 里写成「文件存在就签名、不存在就退回默认」的条件配置，这样既保证了签名恒定，也不影响他人 clone 后编译。核验用 `keytool -printcert -jarfile <apk>` 比对 SHA1，比重新装一遍快得多。

19. **Activity 的「一次性指令」extra，读到后要立刻消费，并配 `savedInstanceState == null` 守卫。** 通过 Intent extra 传「打开后自动展开某一项」这类指令时，不 `removeExtra` 的话，Activity 被系统回收后重建会读到同一份 extra 再执行一次，用户看到的是「从子页面返回时莫名跳转」。同时还要用 `savedInstanceState == null` 区分「全新打开」（该执行）与「恢复现场」（不该执行）：进程被杀后重建时，intent 是系统重新下发的副本，`removeExtra` 的效果可能已经不在，此时只有这个守卫可靠。两个措施缺一不可。

20. **「抓不到的数据，先想想它在不在 DOM 里」。** 现代教务前端（React / Vue）按需渲染：周视图只挂当前周的课程块，切周次才重新渲染。抓 `innerHTML` 拿到的是「当前视图的快照」，不是「数据库里的全部数据」，肉眼可见 ≠ DOM 内存在。遇到「导入不全」，第一步不是改解析器，而是确认页面上肉眼能看到的课与 DOM 里实际存在的课是否一致；不一致时，要么先驱动页面再抓（点「更多」/ 切周次），要么绕开渲染层直接调接口。同理，排查这类问题的最高效路径不是猜 DOM，而是**把抓取到的原始 HTML 存盘留档**，一份样本胜过十轮截图。

21. **全天日程必须用 UTC 时区 + 午夜边界。** `CalendarContract` 规定 `ALL_DAY=1` 的日程要配 `EVENT_TIMEZONE="UTC"`，且 `DTSTART` 要正好落在某个 UTC 午夜上（取「本地看到的那个日期」的 UTC 零点）。若按「本地时区的午夜」写，部分 ROM 的日历会把它渲染成前一天或后一天，日期整体错位。另外全天循环日程的 `DURATION` 用 `P1D`（整天）。

22. **老构建工具链吃不下高版本字节码的第三方库时，可以「降级重编译」而不是换库或升级工具链。** 症状是 `Failed to transform ... using Jetifier. Reason: null` 或 `Unsupported class file major version`。判断口径：先用脚本读 AAR 里每个 class 的 major version（Java 8 = 52，Java 11 = 55，Java 17 = 61），再扫描它的 `import` 清单看有没有真的用到高版本 API（`java.util.function`、`java.lang.invoke` 之类）。
    若结论是「只是编译时标了高版本、实际只用低版本 API」，就去下载对应 tag 的源码，用本地低版本 JDK 重编一遍，native 库原样复用官方 AAR 里的 `.so`（**先确认 `.so` 的 16KB 页对齐**，Android 15+ 的 64 位设备要求 `p_align = 16384`）。本例里 42 个类重编出来只有 43KB。
    注意：**不要用「把 class 文件头的 major version 从 61 改成 52」这种暴力降级**，Java 11+ 的 class 里可能带 `NestHost` / `NestMembers` 属性，改了版本号运行时会报 `IllegalAccessError`。重编译才是干净的做法。
    还有一点：扫描重编译产物，确认原 AAR 里的类一个都没少（本例 41 个类全在，只多出一个匿名内部类）。

23. **OCR 识别课表，结构靠坐标而不是靠文字顺序。** Tesseract 输出的「词 + 包围盒」里，几何关系比文本本身更可靠：先锚定表头（星期）与节次列，再把其余文字归位。三个实用细节：① 同一格的文字要用「纵向间隙聚类」合并（阈值约 1 倍字高），不能按行号硬切；② 节次行高用最小二乘拟合，个别误识别的数字不会带偏整条轴；③ 一定要有降级路径（表头缺失就按等分推），并**把「用了降级」这件事告诉用户**，而不是悄悄给出可疑结果。识别结果先展示后写库，是这个功能的安全网。

24. **贴着屏幕边缘的滚动控件，会被 Android 10+ 的「边缘返回」手势抢走竖滑动作。** 症状很反直觉：同一个页面里，**靠左边那个滚轮**拨了就整页退出，靠右边那个完全正常；而且拨得越猛（横向漂移越大）越容易触发。排查时先排除代码层面——如果没有导航路径能到那个「跳到的页面」，又是 `startActivityForResult` 拉起的，那结论只剩「本页被 finish 了」，进而锁定到返回键/返回手势。
    修法：`View.setSystemGestureExclusionRects()`（API 29+），把滚轮那一行所在的**整条横向区域**从手势判定区排除，矩形**左右都拓到屏幕边缘**（只给控件自身矩形不够，手指常常从边缘起手）；配合 `post {}` 与 `addOnLayoutChangeListener` 在布局变化后重算。老系统的兜底是**给这排控件加左右 padding**，让它离边缘足够远（约 ≥24dp）。
    注意这与「外层 ScrollView 抢纵向手势」（`requestDisallowInterceptTouchEvent`）是**两个不同的问题**，两者都要处理，但别混为一谈——本项目 v116 修过后者，v123 才修前者。

25. **国产 ROM 的「系统 Provider」可能被厂商整个换掉 AUTHORITY，写死 `CalendarContract.AUTHORITY` 在新机型上等于查了个空库。** 新版 ColorOS 日历（versionCode ≥ 7001000）的日历数据在 `content://com.coloros.calendar/`，原生 `com.android.calendar` 基本只剩 exchange 数据；症状是第三方应用查日历列表永远为空、写日程写进了"没人看"的库。修法是**按候选 AUTHORITY 逐个探测**（厂商私有库优先、原生兜底），探测到哪个库有数据，后续读写就全程用哪个库。另有一个容易混淆的同症状坑：ColorOS 把日历权限细分成「允许全部 / 仅允许创建」，选了「仅允许创建」时系统会在 Provider 层拦截查询，包层面明明有 `READ_CALENDAR` 也照样查出来是空——这两种情况要靠报错文案区分开。

---

## 已知限制 / 待验证

- **拖拽手感未在真机验证过。** 逻辑与编译结果已核验，但长按阈值、滑动判定这些手感参数需要真机上手调。
- 拖拽只支持移动，不支持改变课程时长（拉长 / 缩短）。
- 生日提醒依赖手动设置，暂未接入通讯录。
- **生日板块要三格高（约 237dp）以上的卡片才看得到。** 两格卡片连「一行课程 + 板块」都放不下，这时不显示板块，也不去挤压课程行。
- **负一屏的卡片长什么样由 ColorOS 决定，App 只能影响「数据」。** 已确认 ColorOS 的「日历」类卡片能读到本 App 同步进系统日历的课（周课表形态）。v121 起写入的「每日课表」（每天一条整天汇总）在系统日历 App、日历桌面小部件、以及读系统日历的负一屏卡片上都能看到；但**要在负一屏上放 App 自己的卡片，仍需 OPPO 开放平台的卡片审批**（路径 B）。
- **「每日课表」在负一屏上的具体呈现需真机确认**：取决于你负一屏加的是哪种卡片（「日历 / 日程」类卡片会显示每天的全天汇总；若只有「课程表」周视图卡片，则先在系统日历 App 里看日视图）。
- 日历同步是手动的：改完课表要再点一次「同步到系统日历」。暂未做自动监听与自动重同步。
- **紧凑两列的视觉效果未在真机验证。** 半宽格内 10sp 的时间文字在 4 格宽卡片上刚好放得下，卡片更窄时会被 `ellipsize` 截断。
- **课名过长时会截断成前几行加「…」。** 这是有意为之：格子高度是固定的，优先保住教室名。想看完整课名可以点开课程详情，或者把「课程显示文字大小」调小、「课程格子高度」调大。
- **图片识别的准确率未在真机大规模验证。** 算法针对的是最常见的形态：整页、端正、清晰、带「周一~周日」表头与左侧节次列的课表截图（教务系统页面截图、本 App 自己的课表截图）。倾斜、遮挡、压缩严重的手拍图会明显变差；跨列合并的课程块（同一门课占两列）按最近的一列归位。识别结果先展示、确认后才写库，识别不准就换张图，不会污染现有课表。
- 图片识别不提取教师名（课表格子里通常也不显示），也不会自动合并「同一门课一周上多次」的分段。

---

## 构建说明

工程为 Android 项目（Kotlin / Gradle 5.4.1 / AGP 3.5.3 / compileSdk 29，minSdk 21，targetSdk 29）。

```bash
# 需要 JDK 8
./gradlew assembleNormalDebug
```

产物：`app/build/outputs/apk/normal/debug/app-normal-debug.apk`

> `local.properties` 需要自行创建并指向本机 Android SDK 路径，该文件已被 git 忽略。
>
> `keystore/xiaotang.jks` 是固定签名密钥，同样被 git 忽略（私钥不进公开仓库）。本地若没有这个文件，构建会自动退回系统默认调试签名，编译不受影响；只有需要发布「可覆盖上一版」的 APK 时才必须带上它。
